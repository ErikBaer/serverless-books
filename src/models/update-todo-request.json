{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "updateBook",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "pattern": "^(?!\\s*$).+"
        },
        "unread": {
            "type": "string",
            "pattern": "^(?!\\s*$).+"},
        "coverUrl": {
            "type": "string",
            "pattern": "^(?!\\s*$).+"},
        "amazonUrl": {
            "type": "string",
            "pattern": "^(?!\\s*$).+"
        }
      }
    ,
    "required": ["name", "unread"],
    "additionalProperties": false
  }