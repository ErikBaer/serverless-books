{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "createBook",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "pattern": "^(?!\\s*$).+"
        },
        "topic": 
        {"type": "string",
        "pattern": "^(?!\\s*$).+"
        },
        "coverUrl": {
            "type": "string",
            "pattern": "^(?!\\s*$).+"},
        "amazonUrl": {
            "type": "string",
            "pattern": "^(?!\\s*$).+"
        }
    },
    "required": [
        "name",
        "topic"
    ],
    "additionalProperties": false
  }